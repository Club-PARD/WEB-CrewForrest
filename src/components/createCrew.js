import React from 'react';
import { Link } from 'react-router-dom';

const createCrew = () => {
  return (
    <div>
      <h1>createCrew</h1>
      <p>createCrew page</p>
    </div>
  );
};

export default createCrew;